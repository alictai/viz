class Cat_View {
  Stacked_Bar[] views;
  Data data
  
  
  
  
  Cat_View(Data parsed) {
    views = new Stacked_Bar[3];
    data = parsed;
    
    for(int i = 0; i < 3; i++) {
      views[[i] = new Stacked_Bar(/*data*/);
    
  }
  
  
  
  
  
}
